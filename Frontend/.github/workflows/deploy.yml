name: 🚀 Full Deploy - Angular + NestJS Docker

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # -------------------------------
      # 1️⃣ Clonar código del repositorio
      # -------------------------------
      - name: 🧩 Checkout del código
        uses: actions/checkout@v4

      # -------------------------------
      # 2️⃣ Configurar Node.js (para frontend)
      # -------------------------------
      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: EcoRuta/package-lock.json

      # -------------------------------
      # 3️⃣ Instalar dependencias Angular
      # -------------------------------
      - name: 📦 Instalar dependencias del frontend
        working-directory: ./EcoRuta
        run: npm ci

      # -------------------------------
      # 4️⃣ Compilar Angular (producción)
      # -------------------------------
      - name: 🏗️ Compilar Angular
        working-directory: ./EcoRuta
        run: npm run build -- --configuration production --base-href /joan_site/


      # -------------------------------
      # 5️⃣ Configurar acceso SSH
      # -------------------------------
      - name: 🔑 Configurar acceso SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      # -------------------------------
      # 6️⃣ Subir frontend a Apache
      # -------------------------------
      - name: 🚚 Desplegar frontend
        run: |
          echo "📤 Subiendo frontend al servidor..."
          scp -o StrictHostKeyChecking=no -r EcoRuta/dist/EcoRuta/browser/* ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/tmp/eco_frontend/
          ssh -o StrictHostKeyChecking=no ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} '
            echo "🧹 Limpiando /var/www/joan_site..."
            rm -rf /var/www/joan_site/*
            ls -l /tmp/eco_frontend/
            mv /tmp/eco_frontend/* /var/www/joan_site/
            chown -R www-data:www-data /var/www/joan_site
            schmod -R 755 /var/www/joan_site
            systemctl restart apache2 || sudo systemctl restart nginx
            echo "✅ Frontend desplegado correctamente."
          '

     